name: klamben_build
description: Build script that fans out canonical skill + commands to harness-specific layouts.
publish_to: none
version: 0.0.1

environment:
  sdk: ^3.3.0

dependencies:
  path: ^1.9.0
  yaml: ^3.1.2

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
